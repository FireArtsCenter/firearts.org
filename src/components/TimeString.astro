---
interface Props {
	// @// TODO:  <18-02-26, john> - ignoring 'any' until I refactor this component
	// eslint-disable-next-line @typescript-eslint/no-explicit-any
	time: any;
}

const { time } = Astro.props;
const timeString = `${time.fields.dayOfWeek} ${time.fields.timeRange}`;
---

<span>{timeString}</span>
