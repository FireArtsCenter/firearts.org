---
import Button from '../../components/Button';
import Layout from '../../layouts/Layout.astro';
import { org } from '../../constants/constants';
---

<Layout>
	<section>
		<p>
			If you are interested in bronze or iron casting (using the lost-wax
			method), metal furniture design, armor forging, stone carving, figure
			sculpture, figure drawing or just want to check out the Fire Arts Center
			of Chicago’s studio, please contact us and let us know.
		</p>
		<div class='mt-6 flex justify-around'>
			<Button as='link' styleType='primary' href={`mailto:${org.email}`}>
				Email the Fire Arts Center
			</Button>
			<Button
				href={`tel:+1${org.phone.split('.').join('')}`}
				styleType='secondary'
			>
				Call {org.phone}
			</Button>
		</div>
	</section>
</Layout>
